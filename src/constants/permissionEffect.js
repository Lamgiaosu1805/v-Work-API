const PERMISSION_EFFECT = Object.freeze({
  ALLOW: "allow",
  DENY: "deny"
});

const PERMISSION_EFFECT_VALUES = Object.values(PERMISSION_EFFECT);

module.exports = { PERMISSION_EFFECT, PERMISSION_EFFECT_VALUES };
